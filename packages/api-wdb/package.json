{
  "name": "@duly/api-wdb",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "netlify functions:serve",
    "link": "netlify link --name api-wdb",
    "deploy": "netlify deploy --build --prod --dir=public --site=api-wdb",
    "deploy:test": "netlify deploy --build --prod --dir=public --site=api-wdb-test"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.5"
  },
  "devDependencies": {
    "@netlify/functions": "^1.6.0",
    "prettier": "^2.4.1",
    "typescript": "~4.5.5"
  }
}
